<Window
    x:Class="RoboCopyGUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:RoboCopyGUI"
    mc:Ignorable="d"
    Title="WinUI 3 File Copier">

    <Grid Padding="30" RowDefinitions="Auto, *, Auto, Auto, Auto">

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="File Transfer Utility" Style="{StaticResource TitleTextBlockStyle}" FontWeight="Bold"/>
            <TextBlock Text="Drag and drop sources and select a destination." Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7"/>
        </StackPanel>

        <Border x:Name="DropArea" 
                Grid.Row="1"
                Background="Transparent"
                BorderBrush="{ThemeResource SystemControlDisabledAccentConfigurableBrush}"
                BorderThickness="2" 
                CornerRadius="8"
                AllowDrop="True"
                DragOver="DropArea_DragOver"
                Drop="DropArea_Drop">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="8" Grid.Row="0">
                    <FontIcon Glyph="&#xE8B7;" FontSize="48" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                    <TextBlock Text="Drop Files or Folders Here" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="FileCountText" Text="0 items selected" Style="{StaticResource CaptionTextBlockStyle}" HorizontalAlignment="Center"/>
                </StackPanel>

                <ListView x:Name="SourceListView"
                          Grid.Row="1"
                          ItemsSource="{x:Bind SourceItems, Mode=OneWay}"
                          SelectionMode="None"
                          IsItemClickEnabled="False"
                          MaxHeight="220"
                          Margin="0,24,0,0"
                          BorderThickness="0"
                          IsTabStop="False">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:SourceItem">
                            <Grid ColumnDefinitions="*, Auto" Padding="6,4">
                                <TextBlock Text="{x:Bind Path}"
                                           TextTrimming="CharacterEllipsis"
                                           TextWrapping="WrapWholeWords"
                                           Foreground="{x:Bind TextBrush, Mode=OneWay}"
                                           VerticalAlignment="Center"/>
                                <FontIcon Glyph="&#xE73E;"
                                          Grid.Column="1"
                                          FontSize="16"
                                          Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                          Visibility="{x:Bind CompletionVisibility, Mode=OneWay}"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Button x:Name="ClearListBtn" Grid.Row="2"
                        Content="Clear list"
                        HorizontalAlignment="Right"
                        Margin="0,12,0,0"
                        Click="ClearSources_Click"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Margin="0,20,0,0" Spacing="10">
            <TextBlock Text="Destination Path" Style="{StaticResource BodyStrongTextBlockStyle}"/>
            <Grid ColumnDefinitions="*, Auto">
                <TextBox x:Name="DestPathText" PlaceholderText="Select a folder..." IsReadOnly="True" VerticalAlignment="Center"/>
                <Button x:Name="BrowseDestBtn" Grid.Column="1" Content="Browse" Click="SelectDest_Click" Margin="10,0,0,0"/>
            </Grid>

            <CheckBox x:Name="DeleteSourceCheck" Content="Delete source files after successful copy (Move mode)"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,20,0,20" Spacing="10">
            <TextBlock x:Name="StatusText" Text="Ready" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPlaceholderTextForegroundBrush}"/>
            <ProgressBar x:Name="CopyProgressBar" Minimum="0" Maximum="100" Value="0" IsIndeterminate="False" Height="10"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="12">
            <Button x:Name="StartBtn" Content="Start Copy" Click="StartBtn_Click" Style="{StaticResource AccentButtonStyle}" Width="100"/>
            <Button x:Name="PauseBtn" Content="Pause" Click="PauseBtn_Click" IsEnabled="False" Width="100"/>
            <Button x:Name="CancelBtn" Content="Cancel" Click="CancelBtn_Click" IsEnabled="False" Width="100"/>
        </StackPanel>

    </Grid>
</Window>